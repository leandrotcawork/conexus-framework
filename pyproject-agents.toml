# Consumer pyproject — placeholder for Phase 6.4
# Depends on the conexus framework package.
[project]
name = "conexus-agents"
version = "0.1.0"
description = "Conexus consumer — Ana, Pesquisador agents"
requires-python = ">=3.11"
dependencies = [
  "conexus",  # framework package — filled in Phase 6.4
  # Filled in Phase 6.4 — consumer deps (python-telegram-bot, google-api-python-client, etc.)
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agents", "adapters"]
